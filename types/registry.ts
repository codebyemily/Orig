export interface RegistryEntry {
  id: string
  filename: string
  signedFilename: string
  thumbnail: string
  signedImageDataUrl: string
  timestamp: string
  imageHash: string
  artistId: string
  displayName: string
}